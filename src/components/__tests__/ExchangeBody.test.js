import React from 'react'
import '@testing-library/jest-dom/extend-expect'



describe('Exchange Body', () => {
    it.todo('only loads in CurrencyForm when data fetch is complete', () => {})
    it.todo('only loads in ExchangeResults when equivalent value valid', () => {})
})