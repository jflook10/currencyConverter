import React from 'react'

describe('Exchange Body', () => {
    xit('only loads in CurrencyForm when data fetch is complete', () => {})
   xit('only loads in ExchangeResults when equivalent value valid', () => {})
})